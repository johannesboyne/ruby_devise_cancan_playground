%h1 Hello!

- unless admin_signed_in?
  %p
    = link_to 'Sign up as Admin', new_admin_registration_path
  %p
    = link_to 'Sign in as Admin', new_admin_session_path

- unless user_signed_in?
  = link_to 'Sign up as User', new_user_registration_path
  = link_to 'Sign in as User', new_user_session_path

- if user_signed_in?
  = link_to 'Token Authentication Example', token_path(auth_token: current_user.authentication_token)

%p
  Hello World!
